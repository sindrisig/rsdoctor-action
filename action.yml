name: '@yifancong/bundle-action'
description: 'A simple demo for checking compressed size differences'
branding:
  icon: 'archive'
  color: 'purple'
inputs:
  github_token:
    description: 'The GITHUB_TOKEN secret'
    required: false
    default: ${{ github.token }}
  file_path:
    description: 'Path to the file to upload (relative to project root)'
    required: true
  target_branch:
    description: 'The target branch to compare against (defaults to main)'
    required: false
    default: 'main'
  dispatch_target_branch:
    description: 'Override target branch when triggered by workflow_dispatch'
    required: false
    default: ''
    type: string
  baseline_search_depth:
    description: 'How many parent commits to walk back when looking for a baseline with uploaded artifacts. Increase this if the action only runs on path-filtered pushes and may have gaps on the target branch.'
    required: false
    default: '50'

runs:
  using: 'node20'
  main: 'dist/index.js'